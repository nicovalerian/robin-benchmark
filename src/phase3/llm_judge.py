# LLM-as-judge removed. ROBIN Phase 3 uses two evaluation streams only:
#   Stream A: Rule-based constraint checker (constraint_checker.py)
#   Stream B: Semantic scorer — ROUGE-L + BERTScore (semantic_scorer.py)
